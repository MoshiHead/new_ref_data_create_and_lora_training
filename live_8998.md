# PersonaPlex + IMTalker: Exact `try_vad2` 8998 Package

This archive reproduces the server verified live on 2026-08-23. The packaged
Python server, calibrated browser UI, idle video, and prompt runtimes were copied
directly from the process running on port 8998. Large checkpoints download during
preparation.

## Requirements

- Ubuntu x86-64 with an NVIDIA RTX 5090 or compatible CUDA GPU.
- Internet access and sufficient disk space for PersonaPlex and IMTalker.
- A Hugging Face account approved for `nvidia/personaplex-7b-v1`.
- A read token belonging to that approved account.
- Port 8998 exposed by the machine or cloud provider.

## Prepare

```bash
unzip speech2avatar_imtalker_personaplex_8998_try_vad2.zip
cd speech2avatar_imtalker_personaplex_8998_try_vad2
./prepare_imtalker_personaplex.sh --hf-token YOUR_HUGGING_FACE_TOKEN
```

Preparation creates `.venv`, installs PyTorch 2.8.0 CUDA 12.8 and runtime
dependencies, downloads checkpoints, installs PersonaPlex's bundled Moshi, and
runs the deployment preflight. Interrupted Hugging Face downloads are resumable.

Add `--with-search` to also set up online search:

```bash
./prepare_imtalker_personaplex.sh --hf-token YOUR_HUGGING_FACE_TOKEN --with-search
```

That additionally installs the `peft`/`transformers` pins, an **isolated** copy
of the upstream Kyutai `moshi` package for the STT submodel, and the reference
LoRA adapter. The isolation is not optional: PersonaPlex ships a *fork* of
`moshi` under the same top-level import name, the STT model needs the genuine
upstream package for `moshi.models.loaders.CheckpointInfo`, and two packages
cannot both own `sys.modules["moshi"]`. It is therefore installed into
`checkpoints/stt/` and loaded under the private alias `moshi_stt`. Installing it
into site-packages instead would collide with the fork.

## Run The Exact 8998 Configuration

```bash
./run_imtalker_personaplex.sh
```

The process stays in the foreground. Open the provider HTTPS URL for port 8998
after Uvicorn reports that it is listening. Select a physical GPU with:

```bash
CUDA_VISIBLE_DEVICES=1 ./run_imtalker_personaplex.sh
```

With online search enabled (see the section below):

```bash
ENABLE_SEARCH=1 WEB_SEARCH_API_KEY=<tavily-key> ./run_imtalker_personaplex.sh
```

## Change The Text Prompt

The default prompt is stored at:

```text
IMTalker/prompts/Robert_8998_default.txt
```

Edit that file, or point to a different text file without changing source code:

```bash
TEXT_PROMPT_FILE=/absolute/path/to/my_prompt.txt \
./run_imtalker_personaplex.sh
```

For a short one-off prompt:

```bash
TEXT_PROMPT='You are Robert from RB Labs. Answer every part clearly.' \
./run_imtalker_personaplex.sh
```

`TEXT_PROMPT` takes precedence over `TEXT_PROMPT_FILE`. The prompt is wrapped as
a PersonaPlex system turn:

```text
<|im_start|>system
PROMPT_TEXT<|im_end|>
```

Restart the server after changing the prompt. The text is read once at startup.

## Cache A Long Prompt

Caching is optional and disabled by default because the verified 8998 process
runs with normal prompt replay. To cache a long prompt after its first warmup:

```bash
PROMPT_CACHE=1 \
TEXT_PROMPT_FILE="$PWD/IMTalker/prompts/RB_Robert_System_Prompt_full.txt" \
./run_imtalker_personaplex.sh
```

Healthy cache logs are:

```text
captured post-warmup native LM streaming state cache
restored post-warmup native LM streaming state cache hit=...
```

This cache is an in-memory snapshot of PersonaPlex/Moshi's post-prompt streaming
state. It is not a `.pt` file and is not persisted across process restarts. The
cache key includes the wrapped text prompt and voice prompt path. Changing the
text or voice requires restarting the server so it builds the correct cache.

For maximum behavioral fidelity to the currently verified 8998 server, leave
`PROMPT_CACHE=0`. Enable it only when repeated long-prompt replay at every Start
is measurably delaying sessions, then compare multi-turn answer quality before
deployment.

## Change The Voice Prompt

The default is `Robert_5.pt`. Any `.pt` present in
`checkpoints/personaplex_bnb4/voices/` can be selected:

```bash
VOICE_PROMPT=VARM3.pt ./run_imtalker_personaplex.sh
```

## Exact Runtime Configuration

```text
Server                     imtalker_personaplex_try_vad2_8998.py
PersonaPlex                BNB4, continuous native state
Voice                      Robert_5.pt
Reference image            3robert.jpeg
Browser calibration        scale 2.0; offset X 0.024; offset Y -0.047; rotation 0
Idle media                 robert_idle_10s.mp4 with A/V start barrier
JPEG quality               82 (JPEG_QUALITY)
Chunk                      2.0 seconds / 25 hidden steps / 50 frames
FPS                        25
Generator                  fullgen_static_2s_6400_resume/last.ckpt
Adapter                    personaplex_unitalk_strict2s_2gpu_15k/last.pt
CFG / NFE                  1.24 / 3
Renderer                   FP32, sub-batch 10 (RENDER_SUB_BATCH)
Frame backpressure         32
Audio                      native PersonaPlex Opus, paced every 80 ms
Transport                  separate conversation/audio and video WebSockets
Prebuffer                   1 generated chunk, browser buffer 160 ms
Motion blends              reference 0, prior-noise 0, transition 0
Noise                      shared, seed 42
Blink composite            enabled
Prompt-state cache         disabled by default; optional with PROMPT_CACHE=1
Silero/external VAD        disabled
Mic backlog cap            6.0 s (MAX_INPUT_BUFFER_SEC)
Reference LoRA             merged into base weights (MERGE_REF_LORA)
STT/VAD placement          own thread + CUDA stream (STT_INLINE=0)
Online search              off by default (ENABLE_SEARCH=1 to enable)
Structured logs            $ROOT/logs (LOG_DIR)
```

## Online Search, Context Injection, And The Thinking Sound

Off by default. `ENABLE_SEARCH=0` appends no search flags at all and reproduces
the plain conversational launch exactly: no STT model, no router, no extra
VRAM, and no per-chunk cost.

```bash
ENABLE_SEARCH=1 WEB_SEARCH_API_KEY=<tavily-key> ./run_imtalker_personaplex.sh
```

### What happens per turn

```text
user speech
  -> STT transcript (kyutai/stt-1b-en_fr, isolated upstream moshi package)
  -> transcript sanity gate (writing system, then language)
  -> ROUTING
       Tier 0  regex rules on the GPU thread, microseconds, deterministic
       Tier 1  one forward pass of the shared instruct model, background thread
     |
     +-- no search needed -------------------------------------------------
     |     nothing is injected, nothing is played, the live context is
     |     untouched, and the model answers from its own knowledge. This is
     |     the fast path and it costs essentially nothing: the rules pass is
     |     measured in microseconds and never touches the LM.
     |
     +-- search needed ----------------------------------------------------
           thinking sound starts  (assets/ai-thinking-sound.wav, looped)
           <lookup> injected      ("please wait", so the model does not
                                   answer from thin air)
           text stream held to zero_text_code, so the model composes NOTHING
           web search -> relevance floor -> clean -> compress to ONE sentence
           -> normalize to spoken form
           thinking sound stops, <ref> injected, model speaks the answer
```

The thinking sound plays **only** while a search is genuinely reaching the
network. It never plays for a turn the model answers itself, and never while
the router is still deciding: in both of those cases the model is about to
speak within a few hundred milliseconds, and a filler clip would only delay and
mask a reply that needed no waiting.

Two details that are easy to get wrong and are deliberate here:

* **The conversation is never reset.** `<lookup>` and `<ref>` are force-fed
  through `lm_gen.step(text_token=...)` with no `reset_streaming()` call, so the
  shared KV cache -- everything said so far -- stays intact. This is context
  *injection*, not a context *swap*.
* **The model is held silent for the whole search.** Muting only the outgoing
  audio is not enough: the model keeps generating text behind the filler and is
  already mid-sentence with an invented figure by the time the real one lands.
  `--suppress_text_during_search 1` forces its own `zero_text_code` instead.

### The reference LoRA

`Darknsu/helium_lora_v1`, downloaded by `prepare_imtalker_personaplex.sh
--with-search` into `checkpoints/rag_lora/lora/` together with the matching
`adapter_config.json` (the dataset repo publishes only the weights).

It is applied **unmerged**, QLoRA-style, on top of the 4-bit base -- and the
*where* matters as much as the *what*: it is loaded in `MoshiOnlyEngine.__init__`
after `loaders.get_moshi_lm(...)` and **before** `LMGen(...)`. PEFT mutates the
LM's submodules in place, so CUDA-graph capture must happen afterwards or the
captured graph runs the unmodified forward pass and the adapter silently does
nothing. Pass `--merge_ref_lora` to merge it into the base weights instead.

This adapter is what makes context injection work at all: the base model does
not reliably act on text injected mid-stream, and this LoRA was trained to
consume exactly the `<lookup>`/`<ref>` tags this path emits.

### What gets injected, and why numbers stay as digits

Whatever reaches the `<ref>` block is what PersonaPlex reads. It is a speech
model, so it converts `$309.32` into "three hundred nine dollars and thirty-two
cents" in its **audio decoder** — reliably, because copying digits is all the
language model has to do.

Spelling numbers out before injection moves that conversion into the model
itself, and it is not reliable there. Measured across the three search turns in
`detailed_20260908_030350.log`:

| Injected | Spoken back |
| --- | --- |
| "three hundred nine dollars and thirty-two cents" | **"$39.32"** — magnitude dropped |
| "one hundred twenty-two **euros** and sixty-four cents" | **"$122.64"** — currency changed |
| "three hundred thirty-eight dollars and seventy-two cents" | "$338.72" — correct |

Two of three wrong, on a conversion the audio decoder was already doing for
free. The old pipeline injects raw digits and never normalises, which is why it
reads prices back correctly.

So `IMTalker/speech_text.py` now does two separable jobs, and only one of them
runs by default:

```text
always      **bold**, [1], [link](url), quotes, brackets  ->  removed
always      $309.32, €122.64, 25%                         ->  kept verbatim
opt-in      $325                                          ->  Three hundred twenty-five dollars
```

Turn the second on with `SPOKEN_FORM_NUMBERS=1` (`--spoken_form_numbers`) if you
would rather have words in the injected text and accept the risk.

Note the ordering inside `ContextCompressor.compress`: the grounding/overlap
check runs on the RAW answer and cleaning happens after it, because a rewritten
answer shares few tokens with the source passage and would otherwise fail its
own relevance gate.

### Turn boundaries with STT on its own thread

Moving STT off the GPU thread changed one thing beyond timing, and it had to be
put back. `_utterance_start_audio_text_len` marks where the previous turn's
reply ends inside `audio_text`. The worker reaches a given frame later than the
GPU thread produced it, so sampling `len(self.audio_text)` at that point records
a position **past** the start of the model's next reply, and the answer recorded
for the turn begins mid-sentence:

```text
MODEL SAID: "risky, especially for someone new to investing."
MODEL SAID: "few things to look at, but the US dollar is definitely ..."
```

`_stt_submit` now queues each frame together with the `audio_text` length as of
that moment, and `_stt_forward_locked` uses the value that travelled with the
frame. Inline mode (`STT_INLINE=1`) samples it directly, as before, since there
the frame is the current one.

### Not speaking before being spoken to

At session start the model free-runs from the system prompt with nothing to
react to, and reads fragments of it aloud:

```text
USER ASKED: ""
MODEL SAID: "<0x0A> When < you a, speak it clearly, keep it brief, ..."
```

That is not only noise — it commits the model to a topic before the first real
question arrives. The model's words are now withheld until STT reports the
user's first real word, using the same native silence token that already covers
a search in flight, so audio, hidden states and chunk cadence are untouched.

It needs STT, since that is what detects the first word; with STT absent the
gate cannot arm. `WAIT_FOR_USER=0` (`--no-wait_for_user`) restores the old
behaviour if you want a greeting before the user speaks.

### How stale the audio may be

`MAX_INPUT_BUFFER_SEC` is not only a drop threshold — it is the ceiling on how
old the audio the model is answering may be. It is back to **2.0s**, the old
pipeline's value. At 6.0 the model can be replying to a question that finished
six seconds ago while the user has already moved on, and measured first-word
latency reached 16.1s and 47.6s.

## The Two Log Files

Written to `$LOG_DIR` (default `$ROOT/logs`), one pair per server run, never
overwritten. Every line carries a millisecond timestamp, and both files use the
same `YYYY-mm-dd HH:MM:SS.mmm` format so an event in one can be lined up against
an event in the other by timestamp alone.

**`system_<session>.log`** -- what the process *is*:

```text
process        pid, python, platform, argv, CUDA_VISIBLE_DEVICES, PYTHONPATH
torch / gpu    torch + CUDA version, device name, VRAM, capability
configuration  every resolved runtime option (API keys shown as <set>)
paths          each checkpoint/asset with its size and whether it exists
model_loaded   each model: WHERE it was loaded from, device, dtype,
               quantization, parameter count, load time
lora_loaded    adapter path, merged or not, rank, alpha, target modules
video          fps, frames per chunk, chunk seconds, sub-batch, JPEG quality,
               codec, buffer_ms, prebuffer, backpressure
active_work    periodic: render/fm cost, mic backlog, queue depth, GPU memory
server_ready   total startup time with a big-to-small stage breakdown
```

**`detailed_<session>.log`** -- what was *said*. One self-contained block per
turn (plus `conversation_<session>.log`/`.jsonl` carrying the same events as a
compact per-stage trace):

```text
COMPLETE TURN REPORT
    1. USER ASKED
    2. ONLINE SEARCH        yes/no, who decided (rules or model), score, reason
    3. SEARCHED FOR         the exact query and provider
    4. SEARCH RESULTS       every result kept, with score and source URL
    5. SUMMARY              the injected summary, plus the model's raw wording
                            before spoken-form normalization
    6. INJECTED INTO MODEL  the exact <ref> text and its token count
    7. MODEL SAID           the reply
    8. TIMING               question to first spoken word, question to answer
                            ready, and every stage (rule_route, router,
                            web_search, compression, lookup_inject, ref_inject)
                            with its share of the total
```

A turn's report is written when the *next* utterance ends -- that is the first
moment the reply is known to be complete. For live latency during a call, watch
the `SPOKE` line instead: it fires on the first audible audio and is the only
honest end-to-end number for a full-duplex model.

## If The Avatar Goes Silent

The conversation log showing perfect answers tells you nothing about whether
anything was audible. The text stream and the audio stream are independent: one
text token is produced per model step regardless of how long that step took, so
`detailed_*.log` records what the model **decided**, never what the browser
actually **received**. Diagnose silence from `system_*.log` and
`live_server.log`, in that order.

### First: did the GPU producer die?

This is the failure that has actually occurred, and it silences everything
instantly and permanently. `_gpu_producer_thread` is the only source of both
frames and audio. If it raises, Moshi, STT, search and the websocket all keep
running, so both structured logs keep looking healthy while the browser falls
back to the idle video loop and plays nothing.

```bash
grep -E "THREAD-DIED|THREAD-RESTART|RENDER-OOM|VRAM" live_server.log
grep -E "component_failed .*thread:|render_oom|vram_headroom" logs/system_*.log
```

A dead producer now writes `component_failed | thread:gpu-producer` into
`system_*.log` with the full traceback, and is restarted up to three times
before it is left down. It is worth checking this first because for a long time
it was invisible: the thread had no top-level exception handling, so the
traceback went to stderr and nowhere else.

### The renderer is the largest allocation in the process

The observed crash was a CUDA OOM inside the renderer, and it only appeared with
`ENABLE_SEARCH=1` — not because search is slow, but because the search models
take the headroom the renderer needs:

| Resident | VRAM |
| --- | --- |
| PersonaPlex 7B (bnb-4bit) + Mimi | 11.60 GB |
| \+ STT 1B, router/compressor 1.5B, reference LoRA r=128 | 15.04 GB |
| \+ FM generator and FP32 renderer | ~26.4 GB of 31.4 GB |

That left about 5 GB, and the renderer's cross-attention at resolution 64 wanted
it in **one block**: `batch x 8 heads x 4096 x 4096 x 4 bytes` is exactly 5.00
GiB for a 10-frame sub-batch. It did not fit, the producer died, and the avatar
never spoke again.

Two of those allocations were avoidable and are now gone:

- The self-attention in `SynthesisNetwork` built the full map and threw it away
  (`attn_output, _ = self.attn(...)`). It now uses
  `F.scaled_dot_product_attention`, which never materialises it.
- The cross-attention map is only ever consumed as `attn.mean(dim=1)`. It is now
  accumulated one head at a time, so the peak is one head plus the running mean
  rather than all eight heads at once. Below
  `IMTALKER_ATTN_HEAD_CHUNK_BYTES` (1 GiB) the original batched path is kept,
  because eight sequential matmuls are not worth a few hundred MiB.

Renderer peak at resolution 64, per sub-batch:

| sub-batch | before | after |
| --- | --- | --- |
| 8 frames | 4.00 GiB | 1.00 GiB |
| 10 frames | 5.00 GiB | 1.25 GiB |

`RENDER_SUB_BATCH` is therefore a VRAM setting first and a latency setting
second. It defaults to **8**, matching the old pipeline's `start_winner_live.sh`.

An OOM is also no longer fatal: `_render_motion_oom_safe` halves the sub-batch
and retries down to a single frame, which is exactly equivalent because the
frame cursor and eye-blink phase advance per frame. Only if one frame will not
fit is a chunk dropped. A dropped chunk is a visible glitch; a dead producer is
a dead avatar for the rest of the session.

`vram_headroom` in `system_*.log` records what is left once every model is
resident, and says so when it is tight.

### Second: is the pipeline keeping up?

The **real-time factor** (`rtf=`) is audio seconds consumed per wall second,
printed on every `[liveTryStudio]` line.

```text
rtf >= 1.00   the pipeline keeps up with the microphone
rtf <  1.00   it is falling behind PERMANENTLY, because the input arrives at
              exactly real time and never slows down
```

Below 1.0 the failure is not graceful. The audio sender paces on a fixed 80 ms
grid (`target_t = epoch + idx * 80ms`), so a producer that cannot supply 80 ms of
audio per 80 ms of wall clock leaves the browser's worklet permanently
under-run — and an under-running worklet outputs **silence**, not choppy speech.

A `realtime_factor_low` entry in `system_*.log` is the evidence for this. If
there are none, the pipeline was keeping up and the cause is elsewhere; do not
tune latency knobs on the assumption that it was not.

Costs, in order:

| Cause | Symptom | Control |
| --- | --- | --- |
| STT/VAD running **inline** on the GPU thread | rtf drops when STT loads | `STT_INLINE=0` (default) |
| Router/compressor resident on the same GPU | VRAM pressure at load | `COMPRESSOR_DEVICE=cpu` |
| Renderer sub-batch too large for the free VRAM | OOM, not low rtf | `RENDER_SUB_BATCH` (default 8) |

STT is the main one. It is a 1B model plus a second Mimi encode, and reading its
VAD score and token buffer back to the host forces two device syncs. Run inline,
all of that lands inside the same 80 ms budget as the 7B step and the renderer.
It now runs on its own thread and CUDA stream; the GPU thread only hands over a
frame reference (well under 0.1 ms). If that worker falls behind it drops the
**oldest** frames and logs it — transcription degrades, audio and video do not.

The reference LoRA is **not** on this list. It runs unmerged, exactly as the old
pipeline runs it, and that pipeline holds real time on the same RTX 5090. See
[The reference LoRA](#the-reference-lora).

### CUDA graph capture and thread ordering

If the system log shows

```text
component_failed | stt_worker (runtime): CUDA error: operation failed due to a
                   previous error during capture
    ... cuDNN error: CUDNN_STATUS_INTERNAL_ERROR   (inside conv1d)
    ... state.graphed_encoder(x)  ->  with cuda.graph(self._graph)
```

that is CUDA graph **capture**, not inference, and the cause is thread
ordering rather than anything about STT itself.

moshi wraps the Mimi encoder and the LM in `CUDAGraphed`, which captures
*lazily* — on the second call, since `warmup_steps` defaults to 1. PyTorch
captures with `capture_error_mode="global"`, meaning **any CUDA work submitted
by any other thread inside the capture window aborts the capture**. Once STT
moved to its own worker thread, its first real frame only arrived when the
browser started streaming — long after the renderer and PersonaPlex producer
threads were busy — so the window was never quiet.

Two changes fix it, and both are about *when*, not *what*:

1. `_warmup_stt()` runs four dummy frames during single-threaded construction,
   forcing every graphed callable to capture while nothing else touches CUDA.
   The worker then only ever **replays**, which is stream-ordered and
   thread-safe. `reset_streaming()` between utterances does not invalidate the
   graphs (`_MimiState.reset()` is a no-op), so capture happens exactly once
   per process.
2. `start_stt_worker()` is called after the first `get_engine()`, not from the
   constructor — the FM/renderer engine is built lazily on the first websocket
   connection and warms up there, so deferring the worker keeps it out of that
   window too.

If capture still fails, the fallback disables CUDA graphs **for the isolated
STT package only** (`moshi_stt.utils.compile._disable_cuda_graph`). That
surgical scope is the whole reason the upstream package is loaded under its own
module alias: setting the `NO_CUDA_GRAPH` environment variable instead would
also disable PersonaPlex's graphs, which are load-bearing for real time. STT
without graphs is slower per frame but it is off the critical path and has a
five-second queue, so it still keeps up. Only if that also fails is STT
disabled — the avatar keeps working and search reports DOWN.

## Latency

Floor for a no-search reply is `2.0s chunk accumulation + fm + render +
160ms browser buffer`. The 2.0s is not tunable: the generator and adapter are
trained on strict 2-second windows.

What is tunable, in order of impact:

| Control | Default | Effect |
| --- | --- | --- |
| `MAX_INPUT_BUFFER_SEC` | `6.0` | Caps the microphone backlog. **The important one.** |
| `RENDER_SUB_BATCH` | `10` | Divides the 50-frame chunk exactly; no padded tail. |
| `JPEG_QUALITY` | `82` | Fewer bytes to encode and to push over the socket. |
| `INCREMENTAL_PUBLISH` | `0` | Opt-in; takes render time off the reply path. |

`MAX_INPUT_BUFFER_SEC` deserves the emphasis. The GPU producer is rate-limited
to exactly real time by `frame_q` backpressure, so it consumes one second of
microphone audio per second of wall clock and can **never** drain a backlog.
Without the cap, any transient stall -- model warmup, one slow render, a client
hiccup -- becomes a permanent, session-long reply delay. That is the mechanism
that turns a pipeline whose floor is a few seconds into one answering ten or
twelve seconds late, and it is why the symptom is a delay that never recovers
rather than one that comes and goes. With the cap, the oldest audio is dropped
(and each drop logged) so the delay stays bounded no matter what happens.

`INCREMENTAL_PUBLISH=1` publishes each render sub-batch as soon as it is
encoded, along with exactly the audio steps those frames cover, instead of
holding the whole chunk. Ordering and A/V alignment are preserved, but the
atomic publication guarantee is not: if a later sub-batch stalls, the browser's
playback clock has already advanced past the earlier ones. Check the per-chunk
`render+jpeg=` numbers in the log are stable before enabling it.

Do **not** set `PREBUFFER_CHUNKS=0`. It looks like a free win and is not:
`prebuffer_ready` gates `_get_media_epoch()`, and that epoch is the anchor the
audio sender paces against (`target_t = epoch + idx * 80ms`). Releasing it
before the first chunk exists anchors the clock seconds ahead of the first
packet, so early packets compute targets already in the past and the sender
burst-sends until the anti-burst spacing catches up. It is also a one-time
session-start cost, not a per-turn one.

To find where a slow turn actually went, read the `TIMING` block of that turn's
report in `detailed_<session>.log`, then cross-check `mic_backlog=` on the
`[GPU][chunk#N]` lines in the server log. A large backlog means the GPU cannot
keep up and no per-turn tuning will help; a small backlog with a large
`web_search` or `compression` stage means the search path is the cost.

## Validate

```bash
./run_imtalker_personaplex.sh --check-only
curl -fsS http://127.0.0.1:8998/ >/dev/null
curl -fsS http://127.0.0.1:8998/assets/robert_idle_10s.mp4 >/dev/null
ss -ltnp | grep ':8998\b'
nvidia-smi
```

The run script refuses to start if required files are missing, protected runtime
files fail checksums, CUDA imports fail, or port 8998 is already occupied.
